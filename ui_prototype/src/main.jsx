import React from "react";
import ReactDOM from "react-dom/client";
import ClarityGridClientApp from "../claritygrid-client-app.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ClarityGridClientApp />
  </React.StrictMode>
);
