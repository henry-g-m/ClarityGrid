[project]
name = "claritygrid-backend"
version = "0.1.0"
description = "FastAPI backend for the ClarityGrid client prototype"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110.0",
  "uvicorn[standard]>=0.29.0",
  "pydantic>=2.7.0",
]

[tool.uv]
package = false
